# 流程状态显示样式逻辑文档

## 概述
流程状态显示通过不同的颜色、字体样式和文本装饰来表示各个步骤的执行状态。

## 涉及的主要文件
- `mainwindow.cpp` - 主要逻辑实现
- `mainwindow_steps.cpp` - 步骤取消逻辑

## 流程步骤定义
系统定义了5个按顺序执行的步骤：
1. `takeEmptyBottle` - 取空瓶
2. `getSolid` - 获取固体
3. `resetXYZ` - 重置XYZ轴
4. `getLiquid` - 获取液体
5. `tightenBottle` - 拧紧瓶子

## 样式定义（mainwindow.cpp 第1074-1077行）

### 1. 已完成样式 (doneStyle)
```css
color: green;
font-weight: bold;
```
- **颜色**：绿色
- **字体**：加粗
- **适用场景**：步骤已成功完成

### 2. 当前执行样式 (currentStyle)
```css
color: #00aa00;
font-weight: bold;
text-decoration: underline;
```
- **颜色**：绿色 (#00aa00)
- **字体**：加粗
- **文本装饰**：下划线
- **适用场景**：步骤正在执行中

### 3. 待执行样式 (pendingStyle)
```css
color: gray;
```
- **颜色**：灰色
- **适用场景**：步骤尚未执行

### 4. 已跳过样式 (skippedStyle)
```css
color: gray;
text-decoration: line-through;
```
- **颜色**：灰色
- **文本装饰**：删除线
- **适用场景**：步骤被跳过或取消




## 状态优先级总结

当一个步骤的状态发生冲突时，按以下优先级应用样式：

1. **已跳过** (最高优先级)
   - 灰色 + 删除线
   - 一旦跳过，无论其他状态如何都显示为跳过

2. **当前执行**
   - 绿色加粗 + 下划线
   - 表示正在执行的步骤

3. **已完成**
   - 绿色加粗
   - 表示已成功完成的步骤

4. **待执行** (最低优先级)
   - 灰色
   - 默认状态

 




## 视觉效果示例

假设执行顺序为：取空瓶 → 获取固体 → 重置XYZ → 获取液体 → 拧紧瓶子

### 场景1：正常执行到"获取液体"
- 取空瓶：✅ **绿色加粗**
- 获取固体：✅ **绿色加粗**
- 重置XYZ：✅ **绿色加粗**
- 获取液体：▶️ **绿色加粗+下划线**
- 拧紧瓶子：⏸️ 灰色

### 场景2：跳过"获取固体"，执行到"获取液体"
- 取空瓶：✅ **绿色加粗**
- 获取固体：❌ ~~灰色+删除线~~
- 重置XYZ：✅ **绿色加粗**
- 获取液体：▶️ **绿色加粗+下划线**
- 拧紧瓶子：⏸️ 灰色

### 场景3：全部完成
- 取空瓶：✅ **绿色加粗**
- 获取固体：✅ **绿色加粗**
- 重置XYZ：✅ **绿色加粗**
- 获取液体：✅ **绿色加粗**
- 拧紧瓶子：✅ **绿色加粗**

## 潜在改进建议

### 1. 样式一致性
- `cancelStep()` 中直接设置样式为 `"color: gray; text-decoration: line-through;"`
- 建议统一使用 `skippedStyle` 常量，避免硬编码

### 2. 状态管理
- 当前使用两个独立的 `QSet` 管理状态
- 可考虑使用枚举类型统一管理步骤状态（Pending/Running/Completed/Skipped）

### 3. 样式定义位置
- 样式字符串定义在函数内部
- 建议提取为类成员常量或配置文件，便于维护和主题切换

### 4. 颜色值
- `currentStyle` 使用 `#00aa00`
- `doneStyle` 使用 `green`
- 建议统一使用十六进制颜色值或命名颜色

---

