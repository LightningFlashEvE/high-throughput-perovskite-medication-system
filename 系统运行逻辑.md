# 高通量钙钛矿药物配方制备系统 — 运行逻辑文档

## 一、系统总览

本系统是一套工业自动化控制软件，驱动机械臂、天平、摇床等硬件，自动完成钙钛矿前驱体溶液的配制。软件采用**事件驱动 + 异步命令队列**架构：上层业务逻辑将操作步骤拆分为命令序列写入队列，底层通信模块按顺序逐条发送并等待硬件响应，全程非阻塞，界面始终保持响应。

```mermaid
graph TD
    User["用户界面 / 主窗口"]
    Analyzer["配方分析器"]
    DB["MySQL 数据库"]
    TCP["TCP 通信核心"]
    Serial["串口 RS-485 ModBus"]
    Balance["天平 TCP"]
    Motors["电机设备群\n02/03/04/05/06/09/0A/0C"]
    Gripper["电爪"]
    Shaker["摇床"]

    User -->|"化学方程式"| Analyzer
    Analyzer -->|"JSON 配方"| User
    User -->|"保存/读取配方"| DB
    User -->|"消息队列"| TCP
    TCP -->|"串行命令"| Motors
    TCP -->|"称重数据"| Balance
    Balance -->|"实时重量"| TCP
    TCP -->|"ModBus 命令"| Serial
    Serial -->|"控制"| Gripper
    User -->|"摇床记录"| DB
    TCP -->|"摇床开关"| Shaker
```

---

## 二、模块列表

| 模块 | 职责 |
|------|------|
| 主窗口 | 用户交互、配方调度、业务流程编排 |
| TCP 通信核心 | 命令队列处理、设备通信、电机轮询、天平称重 |
| 数据库 | MySQL 连接管理、配方持久化、摇床记录 |
| 串口通信 | RS-485 ModBus 协议，驱动电爪等设备 |
| 配方分析器 | 化学方程式解析、前驱体用量计算 |
| 试剂箱 | 15 槽位置管理，记录当前存货 |

---

## 三、主窗口模块

### 3.1 启动初始化

```mermaid
flowchart TD
    A([程序启动]) --> B[读取 data.ini / BoxData.ini 配置文件]
    B --> C[初始化 TCP 对象\n主控 + 天平各一个]
    C --> D[连接 MySQL 数据库]
    D --> E{存在上次中断的配方？}
    E -->|是| F[标记为开机中断状态\n释放摇床占位]
    E -->|否| G[初始化棋盘看板]
    F --> G
    G --> H[加载上次步骤勾选状态]
    H --> I[启动定时器①\n每 1s 刷新时间标签]
    H --> J[启动定时器②\n每 10s 检查摇床是否到期]
    H --> K[启动定时器③\n每 1s 摇床超时兜底保护]
    I & J & K --> L([初始化完成，等待用户操作])
```

### 3.2 配方创建与排队

```mermaid
flowchart TD
    A([用户输入化学方程式]) --> B[配方分析器计算\n各前驱体质量mg + 溶剂用量mL]
    B --> C[生成 JSON 配方数据包]
    C --> D[主窗口接收配方]
    D --> E[创建配方队列条目\n状态=待处理]
    E --> F[保存到数据库\nrecipeQueue 表，状态=0]
    F --> G[从数据库读取第一条待处理配方]
    G --> H[更新配方状态为 1 处理中]
    H --> I[将消息队列导入 TCP 核心]
    I --> J([开始执行])
```

### 3.3 步骤选择与消息队列构建

```mermaid
flowchart TD
    A([用户勾选步骤后点击运行]) --> B[遍历 6 个流程步骤]
    B --> C{该步骤是否勾选？}
    C -->|已勾选| D[调用业务函数\n追加该步骤所有命令到消息队列]
    C -->|未勾选| E[插入跳过标记\nAAskipStep:步骤名]
    D --> F{还有下一步？}
    E --> F
    F -->|是| B
    F -->|否| G[消息队列构建完成]
    G --> H[发送给 TCP 通信核心执行]
```

**取固体步骤举例：**

```mermaid
flowchart TD
    A([取固体步骤开始]) --> B[从数据库查询固体试剂盘位置]
    B --> C[命令：电机04 移至固体盘上方]
    C --> D[命令：电机06 下降取粉]
    D --> E[命令：电机06 上升]
    E --> F[命令：电机05 夹紧]
    F --> G[命令：电机04 回初始位]
    G --> H([步骤命令追加完毕])
```

### 3.4 流程状态显示

```mermaid
flowchart LR
    A[未开始\n灰色] -->|AAstateChange 触发| B[正在执行\n蓝色]
    B -->|执行完毕| C[已完成\n黑色勾选]
    A -->|AAskipStep 触发| D[已跳过\n删除线灰色]
```

### 3.5 紧急暂停

```mermaid
flowchart TD
    A([用户点击紧急暂停]) --> B[停止 TCP 队列\n向所有电机发送急停命令]
    B --> C[弹出对话框\n是否放弃当前配方？]
    C -->|放弃| D[清空 TCP 命令队列]
    D --> E[当前配方状态标记为 8 已放弃]
    E --> F[预加载下一条待处理配方]
    F --> G([等待用户确认继续])
    C -->|继续| H[恢复 TCP 队列]
    H --> I([从中断位置继续执行])
```

### 3.6 摇床管理

```mermaid
flowchart TD
    A([每 10 秒定时触发]) --> B[遍历摇床区 15 个位置]
    B --> C{结束时间 <= 当前时间\n且状态 = 占用？}
    C -->|是| D[发送停止摇床命令]
    D --> E[更新数据库状态为空闲]
    E --> F{还有下一个位置？}
    C -->|否| F
    F -->|是| B
    F -->|否| G([本轮检查完毕])
```

---

## 四、TCP 通信核心模块

这是整个系统的"心脏"，所有与硬件的通信都经过此模块。

### 4.1 命令队列结构

每条命令记录四个字段：

```
┌──────────────┬───────────────────────────────────┐
│ 内容          │ 待发送的字节（如 ">06D0001E240XXXX"） │
│ 格式          │ ASCII 文本 或 十六进制字节流           │
│ 是否等待响应  │ true = 发送后必须等到响应才继续         │
│ 期望响应      │ 收到的数据里必须包含的特征串（如 "06d"） │
└──────────────┴───────────────────────────────────┘
```

### 4.2 队列处理主循环（每 40ms 执行一次）

```mermaid
flowchart TD
    A([每 40ms 触发]) --> B{队列是否为空？}
    B -->|是| C[发出队列空信号\n通知上层加载下一配方]
    C --> Z([退出本次循环])
    B -->|否| D{正在等待某条命令的响应？}
    D -->|是| Z2([等待，本次不取新命令])
    D -->|否| E[取出队列头部命令]
    E --> F{处于跳过步骤模式？}
    F -->|是，且遇到步骤切换命令| G[退出跳过模式\n处理此命令]
    F -->|是，其他命令| H[丢弃命令]
    H --> A
    G --> I{是内部 AA 命令？}
    F -->|否| I
    I -->|是| J[本地处理\n不发送到设备]
    J --> A
    I -->|否| K{距上次发送已满 40ms？}
    K -->|否| L[延迟至 40ms 后发送]
    K -->|是| M[发送命令到设备]
    L --> M
    M --> N{需要等待响应？}
    N -->|是| O[设置期望响应\n启动超时计时器\n进入等待状态]
    N -->|否| A
```

### 4.3 电机轮询机制

```mermaid
flowchart TD
    A([发送电机移动命令]) --> B[设置期望响应，如 06d]
    B --> C[启动 50ms 超时计时器]
    C --> D{50ms 内收到响应\n且与期望匹配？}
    D -->|是| E([成功，继续处理下一条命令])
    D -->|否，超时| F{重试次数 < 400？}
    F -->|是| G[重新发送查询命令\n次数 +1，重置计时器]
    G --> D
    F -->|否，已达 400 次| H{轮询轮数 < 2？}
    H -->|是| I[进入下一轮\n次数清零，继续轮询]
    I --> G
    H -->|否，已完成 3 轮| J[发出超时失败信号]
    J --> K([停止执行，等待人工处理])
```

> 普通命令：超时间隔 90ms，最大重试 300 次（约 27 秒），只有 1 轮。

### 4.4 内部 AA 命令处理

```mermaid
flowchart TD
    A([遇到 AA 前缀命令]) --> B{命令类型}

    B -->|AA0| C[关闭天平打印\n清除重量阈值]
    B -->|AA1| D[开启天平打印]
    B -->|AA2| E[天平去皮]

    B -->|AAsetExpectedWeight:X| F{重量范围}
    F -->|≤ 0.0005mg| G[阈值1=目标-0.0002\n阈值2=目标-0.0002\n阈值3=目标]
    F -->|≤ 0.0110mg| H[阈值1=目标-0.0018\n阈值2=目标-0.0003\n阈值3=目标]
    F -->|> 0.0110mg| I[阈值1=目标-0.0050\n阈值2=目标-0.0010\n阈值3=目标]

    B -->|AAskipStep:步骤名| J[进入跳过模式\n后续该步骤命令全部丢弃]
    B -->|AAstateChange:状态名| K[通知界面更新步骤显示\n若在跳过中且步骤切换则退出跳过]
    B -->|AArecordShakeBedTime:位置:秒| L[写入摇床数据库\n锁定位置，计算结束时间]
    B -->|AAopenShakeBed\nAAcloseShakeBed| M[触发摇床开关]
    B -->|AAleaveTheShaker:X| N[机械臂离开摇床位置X\n释放占位]
```

### 4.5 天平称重机制

```mermaid
flowchart TD
    A([天平推送实时数据]) --> B[提取浮点重量值]
    B --> C[发出实时重量信号\n供状态栏显示]
    C --> D{已设置目标重量？}
    D -->|否| Z([结束])
    D -->|是| E[检查阈值 1]
    E --> F{当前重量 >= 阈值1\n且未触发过？}
    F -->|是| G[标记阈值1已触发\n发出达到重量信号\n通知粗控减速]
    F -->|否| H[检查阈值 2]
    G --> H
    H --> I{当前重量 >= 阈值2\n且未触发过？}
    I -->|是| J[标记阈值2已触发\n发出达到重量信号\n通知精细控速]
    I -->|否| K[检查阈值 3]
    J --> K
    K --> L{当前重量 >= 阈值3\n且未触发过？}
    L -->|是| M[标记阈值3已触发\n发出加料完成信号]
    L -->|否| Z
    M --> Z
```

### 4.6 TCP 连接管理

```mermaid
flowchart TD
    A([TCP 连接断开]) --> B{是手动断开？}
    B -->|是| C([不重连，等待用户操作])
    B -->|否| D{已重连次数 < 5？}
    D -->|是| E[等待递增延迟\n3s / 6s / 9s ...]
    E --> F[发起重连]
    F --> G{重连成功？}
    G -->|是| H([恢复正常通信])
    G -->|否| D
    D -->|否，已尝试 5 次| I[发出错误信号]
    I --> J([等待手动重连])
```

---

## 五、数据库模块

### 5.1 连接方式

通过 ODBC 驱动连接 MySQL（192.168.10.170:3306），数据库名 PhenoLabHT。

每 10 秒执行一次轻量查询验证连接真实可用性。连接状态用彩灯在界面上显示。

### 5.2 配方生命周期状态流转

```mermaid
stateDiagram-v2
    [*] --> 待处理: 用户提交配方\nprocessState = 0
    待处理 --> 处理中: 开始执行\nprocessState = 1
    处理中 --> 已完成: 队列全部执行完毕\nprocessState = 2
    处理中 --> 已放弃: 用户紧急暂停后选择放弃\nprocessState = 8
    处理中 --> 开机中断: 程序异常退出后重启检测到\nprocessState = 9
    已完成 --> [*]
    已放弃 --> [*]
    开机中断 --> [*]
```

### 5.3 关键数据读写时机

| 时机 | 操作 |
|------|------|
| 配方创建 | 保存配方信息和消息队列到数据库 |
| 开始执行配方 | 更新配方状态为 1（处理中） |
| 配方完成 | 更新配方状态为 2（已完成） |
| 配方放弃 | 更新配方状态为 8（已放弃） |
| 程序启动时发现遗留处理中配方 | 更新状态为 9（开机中断） |
| 处理 AArecordShakeBedTime | 写入摇床开始时间、结束时间、占用状态 |
| 摇床到时停止 | 更新摇床状态为空闲 |

### 5.4 主要数据表

```
recipeQueue              配方队列（名称、状态、创建时间）
recipeMessageQueue       配方消息队列（每条操作命令）
shakeBedArea             摇床区域（15 个位置，各自的时间和占用状态）
pan_init                 试剂盘初始位置参数
pan_EmptyBottlePosition  空瓶位置
pan_FinishedProductLocation  成品位置
emptyBottleArea          取瓶区状态（当前取瓶计数）
```

---

## 六、串口通信模块（RS-485 / ModBus）

用于驱动电爪等通过 ModBus RTU 协议控制的设备。

```mermaid
flowchart LR
    A([上层发出控制指令]) --> B[组装 ModBus 帧\n功能码 + 数据 + CRC 校验]
    B --> C[写入串口]
    C --> D[电爪执行动作]
    D --> E[串口收到响应数据]
    E --> F[发出数据信号]
    F --> G([主窗口处理结果])
```

串口参数：8 数据位、无校验、1 停止位、无流控。心跳检测：每 1 秒一次。

---

## 七、配方分析器模块

### 7.1 输入

- 化学方程式（如 `FAI:MAI:PbI2 = 1:1:2`）
- 摩尔浓度（mol/L）
- 目标体积（mL）
- 溶剂及比例（如 DMF 70% + DMSO 30%）

### 7.2 计算流程

```mermaid
flowchart TD
    A([用户输入化学方程式]) --> B[解析方程式\n提取各物质名称和摩尔比]
    B --> C[总摩尔数 = 摩尔浓度 × 体积升]
    C --> D[各前驱体摩尔数 = 总摩尔数 × 各自比例]
    D --> E[各前驱体质量mg = 摩尔数 × 分子量 × 1000]
    E --> F[各溶剂体积mL = 总体积 × 各溶剂比例]
    F --> G[打包为 JSON 格式\n溶质数组 + 溶剂数组]
    G --> H([发送给主窗口])
```

---

## 八、完整配方执行时间轴

```mermaid
sequenceDiagram
    participant U as 用户
    participant MW as 主窗口
    participant DB as 数据库
    participant TCP as TCP 核心
    participant HW as 硬件设备
    participant BA as 天平

    U->>MW: 点击"运行选中步骤"
    MW->>MW: 构建完整消息队列
    MW->>DB: 保存配方（状态=待处理）
    MW->>DB: 更新状态为处理中
    MW->>TCP: 导入消息队列

    loop 每 40ms 处理一条命令
        TCP->>HW: 发送设备命令
        HW-->>TCP: 返回响应
        TCP->>TCP: 匹配期望响应，继续下一条
    end

    TCP->>BA: 启动天平检测（设置目标重量）
    BA-->>TCP: 实时推送重量数据
    TCP->>MW: 达到阈值1，通知减速
    BA-->>TCP: 继续推送
    TCP->>MW: 达到阈值2，继续减速
    BA-->>TCP: 继续推送
    TCP->>MW: 达到阈值3，加料完成

    TCP->>MW: 队列为空信号
    MW->>DB: 更新配方状态为已完成
    MW->>DB: 查询下一条待处理配方
    MW->>TCP: 导入下一配方消息队列
```

---

## 九、关键同步机制

### 9.1 命令串行保证

```mermaid
flowchart LR
    A{m_isWaitingForResponse\n= true？} -->|是| B([阻塞，不取新命令])
    A -->|否| C[取出并发送下一条命令]
    C --> D[设置 m_isWaitingForResponse = true]
    D --> E{收到期望响应？}
    E -->|是| F[设置 m_isWaitingForResponse = false]
    F --> A
    E -->|否，超时重试| C
```

### 9.2 配方串行加载

```mermaid
flowchart LR
    A([当前配方队列清空]) --> B[发出队列空信号]
    B --> C[主窗口收到信号]
    C --> D[从数据库加载下一配方]
    D --> E[导入 TCP 核心]
    E --> F([继续执行])
```

### 9.3 天平称重去重

每个阈值只触发一次（用触发标记数组记录），防止因天平数据抖动导致重复发信号。

---

## 十、错误处理策略

```mermaid
flowchart TD
    ERR([发生错误]) --> B{错误类型}

    B -->|电机长时间未到位| C[重试 400次/轮\n共 3 轮约 60s]
    C --> C2{仍未响应？}
    C2 -->|是| STOP[停止执行\n发出超时报警]
    C2 -->|否| OK([恢复正常])

    B -->|普通命令无响应| D[重试最多 300 次\n约 27s]
    D --> D2{仍未响应？}
    D2 -->|是| STOP
    D2 -->|否| OK

    B -->|TCP 连接断开| E[自动重连最多 5 次\n间隔递增]
    E --> E2{重连成功？}
    E2 -->|是| OK
    E2 -->|否| F[发出错误信号\n等待手动重连]

    B -->|数据库连接异常| G[状态灯变红\n10s 后自动重试]

    B -->|用户紧急暂停| H[急停所有电机\n询问是否放弃配方]
    H --> H2{用户选择}
    H2 -->|放弃| I[清空队列\n标记配方已放弃\n加载下一条]
    H2 -->|继续| J[恢复队列执行]

    B -->|程序异常重启| K[检测遗留处理中配方\n标记为开机中断\n释放摇床占位]
```
